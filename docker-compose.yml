version: '3.8'
services:
    app:
        build: .
        environment:
            - NODE_ENV=production
        ports:
            - '3501:8080'
        volumes:
            # ホスト側または Docker 管理の永続ボリュームを /data にマウント
            - kintore_data:/data
volumes:
    kintore_data:
