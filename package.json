{
  "name": "kintore-root",
  "version": "1.0.0",
  "scripts": {
    "setup": "cd server && npm install && cd ../site && npm install",
    "build:server": "cd server && npx tsc",
    "build:site": "cd site && npm run build",
    "start": "NODE_ENV=production node server/dist/index.js"
  }
}
